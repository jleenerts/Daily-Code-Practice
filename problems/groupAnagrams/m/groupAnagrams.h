#ifndef GROUP_ANAGRAMS_H_
#define GROUP_ANAGRAMS_H_

#include <vector>
#include <map>
#include <iostream>

class groupAnagrams {
    private:
    public:
        std::vector<std::vector<std::string>> group(std::vector<std::string> toSort);

};

#endif