#include "twosum.h"

std::vector<int> TwoSum::twoSum(const std::vector<int>& nums, int target) {
    std::vector<int> result = { 0, 1 };

    nums.capacity();

    int ye = target;
    ye++;

    return result;
}